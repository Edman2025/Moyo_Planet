services:
  moyo-planet:
    build: .
    ports:
      - "4173:4173"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 4173
      DATA_DIR: /data
    volumes:
      - moyo_data:/data
    restart: unless-stopped

volumes:
  moyo_data:
