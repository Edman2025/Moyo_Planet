# 参与贡献

感谢你愿意参与萌友星球的开发。

## 开始开发

1. Fork 仓库并从 `main` 创建功能分支。
2. 使用 Node.js 24 和 `npm ci` 安装依赖。
3. 复制 `.env.example` 为本地环境文件，并按需配置；不要提交任何真实密钥或用户数据。
4. 使用 `npm run dev` 启动本地开发环境。

## 提交要求

- 保持改动聚焦，并说明用户可见的行为变化。
- 新功能或缺陷修复应补充相应测试。
- 提交 Pull Request 前至少运行 `npm run release:check`。
- 不要提交 `data/`、构建产物、上传照片、生成媒体或环境变量文件。
- 涉及第三方图片、字体、音频或代码时，必须确认许可证允许再分发。

## 报告问题

公开缺陷和功能建议可以提交 GitHub Issue。请提供复现步骤、预期结果、实际结果、浏览器或运行环境，以及不包含个人信息的截图或日志。

安全漏洞请遵循 `SECURITY.md`，不要提交公开 Issue。
